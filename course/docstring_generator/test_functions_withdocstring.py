def hi(name: str) -> None:
    """
    This function prints a greeting to the given name.

    Parameters:
        name (str): The name of the person to greet.

    Returns:
    None
     """    
    print(f'Hello {name}')
    return


def return_even(num_list: list) -> list:
    """
    This function takes in a list of numbers and returns a list of even numbers from the given list.
    
    Parameters:
    num_list (list): A list of numbers
    
    Returns:
    evens (list): A list of even numbers from the given list
    
     """    
    evens = []
    for i in return_even:
        if i // 2 == 0:
            evens.append(i)
    return evens
